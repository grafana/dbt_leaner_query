version: 2

sources:

  - name: bq_audit_default_project
    database: "{{ target.database }}"
    schema: bigquery_audit_logs
    tables:
      - name: cloudaudit_googleapis_com_data_access
        description: BQ audit logs from default project.
